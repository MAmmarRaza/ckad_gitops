apiVersion: v1
kind: Service
metadata:
  name: blog-service
  labels:
    app: blog
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30020
  selector:
    app: blog